# Sample check
check:
  local_magnum_proc:
    command: "PATH=$PATH:/usr/lib64/nagios/plugins:/usr/lib/nagios/plugins check_procs -C magnum -u magnum -c 1:1"
    interval: 60
    occurrences: 1
    subscribers:
    - local-magnum-server
